const initialState = {
  availableItems: [
    "как+выучить+js",
    "illustrations",
    "Картинка",
    "anotherPath",
  ],
};

export default (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
